name: jobintel
version: "0.1.0"
profile: jobintel

model-paths: ["models"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: target
clean-targets: [target, dbt_packages]

models:
  jobintel:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
